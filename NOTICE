TProxy
======
内网离线/加速代理站 —— 基于 dnsmasq + Tengine/nginx + Docker 的透明缓存系统

Copyright (c) 2026 grigs28

本文件记录项目使用的第三方组件、许可与重要变更。


第三方组件
----------

| 组件 | 用途 | 许可 |
|---|---|---|
| dnsmasq | DNS 劫持与内网域名解析 | GPL-2.0 / GPL-3.0 |
| nginx (Tengine) | 入口网关、TLS 终结、proxy_cache | BSD-2-Clause |
| Docker / Docker Compose | 容器编排 | Apache-2.0 |
| registry:2 (distribution) | Docker 镜像缓存代理 | Apache-2.0 |
| gitcache (git-cloner/gitcache) | git 仓库缓存 | MIT |
| Alpine Linux | dnsmasq 与 gitcache 的基础镜像 | MIT |
| OpenSSL | 证书签发与验证 | Apache-2.0 |
| Flask | 管理界面后端（计划 D） | BSD-3-Clause |


安全须知
--------

本项目采用 TLS 中间人（MITM）方式实现 HTTPS 透明缓存，涉及自建根 CA：

1. **根 CA 私钥（`proxy/ca/tproxy-ca.key`）绝不可泄露或提交入库。**
   持有者可伪造任意网站证书。该文件已由 `.gitignore` 排除。

2. **安装了根 CA 的客户端，其经代理的 HTTPS 流量可被解密。**
   这是该方案的固有性质，部署前须确保符合所在组织的安全策略。

3. 根 CA 私钥需纳入离线备份流程 —— 丢失将导致所有已接入客户端需重新安装 CA。


变更记录
--------

### 2026-09-24

**全网巡检与客户端加固**（66 台，登录成功 44 台；详见
`docs/2026-09-24-真机巡检与客户端需求分析.md`）

- 客户端兜底块补齐 `print_color` 与 printf 语义 —— 没有分发平台的机器上
  脚本原本会报「未找到命令」且输出全是字面 `%s`
- `repair_dnf_repos` 不再递归进 `backup/`，**不再改写备份文件**
- 无 `dig` 时改用 `nslookup` / `resolvectl` / `getent ahostsv4` 做自检
- `HIJACK_DOMAINS` 由 10 个补齐到服务端实际劫持的 37 个，守卫测试改为
  直接解析 `proxy/dnsmasq/dnsmasq.conf` 当基准
- **`client/dist/tp.client.sh` 纳入版本控制** —— 它被 `.gitignore` 第 9 行的
  `dist/` 误伤，此前从未入库（与阶段 7「三个核心库文件因 `.gitignore`
  规则从未入库」是同一个坑，已是第二次）

**巡检发现、尚未处理的第三方环境问题**（非本项目组件，仅记录）

- 内网 DNS `192.168.0.48` 与 `202.99.192.68` 将 `github.com` 答为 `127.0.0.1`，
  将 `registry-1.docker.io` 答为 Meta 公司的 IP。全网多台机器指向它们，
  表现为 github 不可用；那些 `/etc/hosts` 里的 `github.com` 记录是绕此封锁的补丁
- `.16` 处于半接入状态（DNS 已指向本代理但根 CA 未安装），HTTPS 全部失败

### 2026-09-23

**架构重建**（原 `generator` + `hosts-master.txt` 方案已废弃并移除）

- 确立 DNS 透明劫持 + TLS 终结 + 分类缓存的架构，客户端零配置接入
- 覆盖六类制品：系统包（dnf/yum/apt）、Docker、git、Python、Node.js、Java
- 移除 Nexus（无 Java 构建需求，原占 2.7GB 内存且空转）
- 清除旧 `v3-*` 容器、镜像、缓存数据（约 11.6GB）及 `sys-postgres`/`sys-redis`
- 文档目录由 `doc/` 统一为 `docs/`

**阶段 1-2**（计划 A，标签 `phase-1-2-core-proxy`）

- dnsmasq 域名劫持 + 内网双 IP 解析 + 上游兜底
- 自建根 CA 与域名证书签发
- tengine 443 TLS 终结与系统包透明缓存

**阶段 3-6**（计划 B，标签 `phase-3-6-cache-backends`）

- Docker 镜像缓存（registry:2，上游实例数随劫持域名增减）
- git 仓库缓存（gitcache）
- Python / Node.js / Java 缓存
- 审查修复：git push 被错误路由（改为直连）、私有仓库经共享缓存无认证开放
  （改为带凭据流量直连）、内容协商串味、补齐无缓存收益的劫持域名

**阶段 7**（计划 C，标签 `phase-7-client-setup`）

- `client-setup.sh`：一条命令接入（DNS + 根 CA + Docker/Java/运行时工具级证书）
- 支持 `--rollback` 回滚
- 审查修复：三个核心库文件因 `.gitignore` 规则从未入库、回滚恢复错版本、
  回滚误删用户自有 CA

**阶段 8**（计划 D，标签 `phase-8-management-ui`）

- `manager/`：Flask + 原生 JS 的只读仪表盘（缓存占用 / 劫持规则 / 证书 / 分流入口）
- 审查修复：缓存目录路径映射错误导致 4/6 类被误报为「未启用/空」
  （含 os 缓存已有 75MB 数据却显示未启用）

**新增 NVIDIA NGC 上游**（0.3.6）

- 劫持并缓存 `nvcr.io`（NVIDIA NGC）—— AI 场景的主力仓库
  （CUDA / TensorRT / Triton / NIM / RAPIDS）。
  实测其**公开镜像允许匿名拉取**（匿名 token 可取 `nvidia/cuda` 的 manifest），
  故可以做共享缓存，不必经手任何人的凭据 —— 这是能否共享缓存的前提
- 完整配套：dnsmasq 劫持 + 域名证书 + 独立 registry:2 实例（127.0.0.1:5007）
  + 缓存目录 + 客户端 certs.d 清单。**缺任何一样都只有 MITM 成本、没有缓存收益**，
  这一点写在 `registry.conf` 的注释里
- 实测：4 MB 的镜像层经代理取回后，缓存由 28 KB 增至 4548 KB

**客户端脚本 0.0.3：换根后运行时 CA 包不更新**（0.3.0）

- conda / miniconda 的 `ssl/cacert.pem` 不读系统信任库，需单独追加。
  该逻辑原先按**固定标记串**判重，根 CA 一换就误判「已装过」而跳过，
  客户端继续握着旧根 —— 表现为「换根后 conda/pip 连不上」，
  完全看不出是信任锚没换。实测确认：换根后 conda 包里仍是旧根（2036 年到期）
- 现改为标记带上证书指纹，且追加前先移除此前追加的块；
  覆盖文件时用 `cat` 而非 `mv`，保住原文件的属主与 inode
- 同时修正运行时 CA 包的**权限**：conda 的 bundle 可能只剩 root 可读
  （早先 `remove` 用 `mktemp` + `mv` 落盘，把临时文件的 600 权限带了过去），
  普通用户跑 conda/pip 连自己的 CA 包都打不开 —— curl 只报 exit 77
  （`CURLE_SSL_CACERT_BADFILE`），信息里完全看不出是权限问题。
  实测：本机 `curl` 解析到 `/opt/conda3/bin/curl`，其 bundle 为 600 root:root，
  导致 `client/tests/test-client.sh` 的两项 HTTPS 检查一直失败
- `client/lib/ca.sh` 与 `client/dist/tp.client.sh` 两处各自维护，均已修正，
  并各自加了对应用例（换根后必须是新根、无残留、不破坏原有内容、
  普通用户可读、且「已是最新」分支同样会修权限）

**根 CA 更换**（0.3.0）

- 管理界面可**更换根 CA**：生成新根并自动重签全部域名证书。
  这是全系统破坏性最强的操作（换信任锚，所有证书立即失效、每台客户端都要重装），
  故界面上代价写在弹层顶部、必须先预览、且要手输确认词 ——
  确认词在**服务端**校验，前端那个输入框只是提示、不是安全边界
- 根 CA 默认有效期由 10 年改为 **30 年**；叶子证书保持 10 年。
  根长叶短：叶子到期重签即可（客户端无感），根 CA 换一次要动每一台客户端
- 换根后自动把新根 CA 发布到客户端分发目录（挂载 NAS 的 `grigs/tp`）
- **`gen-ca.sh` 改为原子替换**（安全修复）。旧版直接往 `ca/` 里写：
  `openssl genrsa -out tproxy-ca.key` 会立刻截断现有根私钥，其后任何一步失败
  就**既没有新根也没有旧根** —— 全部客户端与全部域名证书同时失效且无从恢复。
  现改为在工作目录生成后再 `mv` 落位
- 换根前整份备份 `ca/`（含根私钥与全部域名私钥），失败自动回滚；
  回滚也失败时保留备份并明确告知路径
- `.gitignore` 新增 `ca/.rotate-*/` 与 `ca/.rebuild.*/`。这两条必须存在：
  备份目录里是**整套私钥的副本**，回滚失败时它会留在原地，
  漏了规则就等于把它交给下一次 `git add`

**更新日志与版本号单一来源**（0.2.9）

- 新增 `CHANGELOG.md`：**版本号写在该文件第一条 `## [x.y.z]`**，
  管理端顶栏与 `/api/status` 的 `version` 都读它，不再各记一份
- 界面点顶栏版本号可查看该文件，按 Markdown 渲染。渲染器为原生 JS 实现
  （不引第三方库），**先整体转义再套标记**，链接只放行 http/https
- `manager/version.py` 不再保存版本号常量，改为解析 `CHANGELOG.md`

**管理端：证书签发与编辑**（0.2.8）

- `manager/backend/certmgr.py`：在界面内签发 / 重新签发域名证书，
  支持附加域名（SAN）与有效期，并登记进 `ca/domains.txt` 供根 CA 重建时重签
- **`gen-cert.sh` 改为原子替换**（安全修复）。旧版直接往 `certs/` 里写：
  `openssl genrsa -out` 立刻截断旧私钥，`openssl x509 -req -out` 在报错
  **之前**就把旧证书清成 0 字节。即一次失败的重新签发会让该域名的
  `.crt` 与 `.key` 双双变空，HTTPS 彻底失效。现改为在工作目录签好后
  才 `mv` 落位，失败时 `certs/` 一个字节都不动。
- 界面单独展示**根 CA** 到期。此前它不在证书列表里，而它的失效会让
  所有域名同时失效、且每台客户端都必须重装 —— 最要命的一张反而看不见。
- 证书有效期不得超过根 CA 剩余期：超过不会报错，只会得到一张
  「界面显示还有很多天、实际从根 CA 过期起就不被信任」的证书。
- 新增域名默认签 **825 天**而非 3650 天。根长叶短，叶子到期重签即可
  （客户端无感），根 CA 只在最后换一次（客户端必须重装）。
  此前根 CA 与全部叶子同日到期，2036-09-20 会变成一次全局硬失效。
- **静态资源指纹自动计算**：`index.html` 里的 `app.js?v=` 不再手写，
  由 `version.asset_version()` 按文件内容算出。此前靠人记得改，
  忘了的后果是浏览器一直跑缓存的旧脚本，现象却是「新功能没生效」。
- `ca/domains.txt` 支持一行多域名（首个为 CN，其余为 SAN），
  `proxy/deploy.sh` 的签发循环同步适配

**安全须知补充**

- 客户端接入需安装自建根 CA，该机经代理的 HTTPS 流量可被解密
- git 缓存对**匿名只读**流量生效；带凭据的请求直连上游，不落入共享缓存

详见 `docs/stages/` 目录的阶段汇总（22 份）、`docs/specs/`（架构规格）与
`docs/plans/`（四份实施计划）。
