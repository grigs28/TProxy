# 更新日志

**本文件是版本号的唯一来源。** 管理端顶栏显示的版本、`/api/status` 返回的
`version`、以及静态资源指纹，全部从下面第一条 `## [x.y.z]` 取。改版本号只改这一处。

版本号规则：三位十进制、**逐位逢十进位**，不跳号不回退。
详见 [`docs/stages/README.md`](docs/stages/README.md)。
每个版本的完整阶段汇总在 [`docs/stages/`](docs/stages/)。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。
日期为该版本提交到 `main` 的日期。

---

## [0.4.2] - 2026-09-24

### 修复（全来自 66 台真机巡检 —— 单测全绿也照样漏）

- **客户端兜底块缺 `print_color`**（`tp.client.sh` → 0.1.9）。没装分发平台的机器
  （实测 `.9` CentOS 7 / `.17` istoreos / `.78` Debian 12）上，脚本打标题那行
  直接 `print_color: 未找到命令`。同时兜底的 `print_*` 写的是 `echo "$1"`，
  只吃第一个参数 —— 而调用形如 `print_info "… %s …" "$值"`，于是输出里全是
  **字面 `%s`**，数字全丢（最要命的是「metalink（%s 个文件）」看不出几个）。
  本机有 `bas.sh`，所以**在本机永远测不出来**。

- **`repair_dnf_repos` 会改写备份文件**。`dnf_metalink_sources` 用的是
  `grep -r`，递归进了 `/etc/yum.repos.d/backup/` —— 那里的文件 dnf 根本不读，
  报它是误报；更糟的是修复会照单去改，实测把一个未注释 `baseurl` 段的
  `metalink=` 行**删掉了**，备份就此还原不回去。改为只扫顶层，并顺带
  只报 `enabled=1` 的段（全段禁用的 repo dnf 也不会读）。

- **无 `dig` 时自检整段放弃**。`.9`/`.17`/`.78` 都装不上 dig（`.17` 没有包管理器、
  `.9` 是 EOL 的 CentOS 7），而它们其实有替代品。加 `dns_query_tool()` /
  `resolve_first_ip()` / `resolve_via()`。三个解析坑都是真机输出才暴露的：
  `resolvectl` 行尾有 `-- link: eth0`（贪婪 `.*: ` 会匹配到 `link:` 那个冒号）、
  `getent hosts` **只回 IPv6**（该用 `ahostsv4`）、`nslookup` 前几行是它自己的
  服务器地址（通用「找第一个 IP」会把 `127.0.0.1` 当答案）。

- **连同修掉一个会误报的连带问题**：原来无 dig 时 `do_verify` 是**整段 return**，
  所以「公网兜底」那项检查也被跳过了。去掉早退后它会用 dig 直查,
  失败就报「所有备用 DNS 均不可达 —— 代理宕机时将断网」——**凭空吓人**。
  改为区分「查不到」与「工具不支持」（后者跳过）。

- **`HIJACK_DOMAINS` 与服务端漂移**。客户端清单只有 **10** 个，而服务端
  `dnsmasq.conf` 劫持 **37** 个 —— 少掉 `quay.io` `ghcr.io` `mirrors.aliyun.com`
  `nodejs.org` `archive.ubuntu.com` 等 27 个。后果是「检查 `/etc/hosts` 有没有
  钉死劫持域名」**静默漏掉三分之二**。守卫测试原来只比对 `lib/` 与 `dist/`
  两份实现**彼此**一致，两份一起错就永远是绿的 —— 改为**直接解析
  `proxy/dnsmasq/dnsmasq.conf`** 当基准。真机验证：`.92` 现在报出
  `archive.ubuntu.com security.ubuntu.com github.com` 三个，改前只报 `github.com`。

- **`dist/tp.client.sh` 被 `.gitignore` 误伤，从未入库**。第 9 行 `dist/` 是
  Python 模板给根目录构建产物用的，没加前导斜杠 → 任意深度都匹配 →
  把 `client/dist/` 一起吃了。后果：push 里一直没有它（只有 `lib/` 那半进了仓库）、
  NAS 那份成了唯一副本、干净克隆上三组「两份实现不许分叉」守卫直接报红。
  改为锚定根目录 `/dist/`。
  ⚠️ 这与阶段 7 那次「三个核心库文件因 `.gitignore` 规则从未入库」是**同一个坑**。

### 附：巡检与需求分析

`docs/2026-09-24-真机巡检与客户端需求分析.md` —— 66 台（登录成功 44 台）的
形态、接入状态四档、按危害排序的需求清单。

### 已知未处理

- `192.168.0.48`（及 `202.99.192.68`）把 `github.com` 答成 `127.0.0.1`，
  把 `registry-1.docker.io` 答成 Meta 的 IP。**指着它们的机器 github 根本不能用**，
  全网那些 `github.com → 20.205.243.166` 的 `/etc/hosts` 记录是在绕这个封锁。
- `.16` 处于**半接入**状态（DNS 已指 `.18` 但 CA 未装）→ 劫持生效而 TLS 全断，
  `curl` 报 60。这台比没接入更糟。
- 客户端版本 **0.1.6 / 0.1.7 / 0.1.8** 三笔未记入本文件（漏记）。
- `.17`(istoreos) 上 `tp.client.sh` 无输出且退出码 1，原因未定位。

## [0.4.1] - 2026-09-24

### 修复
- **apt 源去重**。上一版把死源**改写**成 `deb.debian.org`，而
  `debian.sources` **本来就提供了同样的源** —— 于是变成重复配置：
  `apt update` 满屏 `W: ... configured multiple times`，且同一份元数据被拉两遍。
  官方文档说得明确：PVE 9 下 `debian.sources` 负责 Debian 基础源、
  `sources.list` 应清空。
  规则改为：**该行声明的 suite 已被 `.sources` 提供 → 删除该行；
  否则才改地址**。backports 这类没被覆盖的会被保留
- 检查判据同步放宽为「有死源 **或** 有重复行」——
  只按死源判断的话，修完死源后残留的重复行再也不会被清理
  （这一条是先改了 repair 的判据、却在真机上没生效才发现的）

### 验证（真机试点）
| 机器 | 形态 | 结果 |
|---|---|---|
| `.91` | PVE | sources.list 只留 backports；**apt update 告警 0、错误 0** |
| `.143` | PVE | 死源 2 行 + bookworm 已修；DNS 从 `.38` 改回 `.18`；apt update 告警 0、错误 0 |
| `.144` | PVE | 重复行 3 条已清；apt update 告警 0、错误 0 |
| `.113` | openEuler SP4 | metalink 7→0、冗余段 3→0、**baseurl 7 行完好且仍是 SP4**、dnf repolist 只剩 4 个正式仓库 |
| `.71` | openEuler SP1 | metalink 7→0、DNS 从被污染的 `202.99.192.68` 改到 `.18`、CA 已装 |

⚠️ 其中 `.71` 与 `.91` 是**先跑出问题再修**的：`.71` 被删了 `runtimes.nvidia`、
`.91` 留下重复配置告警。两处都已修复并加了回归用例 ——
**只有真机试点才暴露得出来，单测全绿。**

## [0.4.0] - 2026-09-24

### 修复（我引入的两处损坏，试点时暴露）
- **Docker 标准化会删掉功能键**。上一版按「保留 data-root、其余替换成标准」
  实现，实测在 `.71`（`ai02`，AI 机器）上把
  `runtimes.nvidia` = `nvidia-container-runtime` **删掉了** ——
  那台机器的 **GPU 容器会直接不可用**；`dns` 也被删。
  根因是把 daemon.json 的键当成一类处理，而它其实分两类：
  **风格**（日志格式、cgroup 驱动）标准化有意义，
  **功能**（runtimes / dns / bip / insecure-registries / data-root …）
  逐机配的，删掉就是故障。
  改为**从原配置原样保留开始，只设标准键 + 只删 registry-mirrors**
- **Java cacerts 会扫进容器镜像层**。`install_java_ca` 里 `find /opt`
  在 Docker data-root 位于 `/opt` 下时（`.71` 是 `/opt/docker`）会扫进
  `overlay2/` —— 那里是**容器自己的** JDK，改它们既无意义（容器不用宿主机的
  认证），又在动别人的镜像层。已排除 `*/overlay2/*`

### 验证
- `.71` 重跑后：`runtimes`(nvidia) / `dns` / `data-root` 全部保留，
  `registry-mirrors` 移除，标准键已设
- 新增回归用例专门守住 `runtimes`/`dns` 不被删
- Java 侧：不再出现 overlay2 路径

## [0.3.9] - 2026-09-24

### 新增
- 客户端脚本**修复别的脚本留下的、会让机器不可用的配置**（`tp.client.sh` → 0.1.3）。
  PVE 机器（`.91-.99` / `.140-.144`，共 14 台，全是 Debian 13）跑过
  `ve/ve.client.sh`，留下三类问题，**都不会当场报错**，只在之后以
  「apt update 失败」「git 连不上」的形式暴露：
  - `/etc/apt/sources.list` 指向 `http://<cacheIP>/repository/debian-proxy/`
    —— 旧 Nexus 路径，架构重建时已下线（实测 `.18` 与 `.36` **都是 000**）
  - 把**已经正确的** PVE 9 源（deb822 + trixie）挪进 backup，
    换成旧单行 `.list` + `bookworm` codename —— 而 PVE 9 基于 Debian 13 (trixie)
  - `/etc/hosts` 覆盖令劫持失效（由 hosts 检查负责）
- **企业版源（需付费订阅）的检测与禁用**。PVE 9 全新安装默认启用
  `enterprise.proxmox.com`；非订阅环境下 `apt update` 返回 **401**，
  且表现为 Proxmox 组件**静默冻结**在安装 ISO 的版本上 ——
  Debian 基础源仍正常，所以安全更新照常，问题极难察觉。
  检测后移进 backup（可逆，拿到订阅挪回来即可）

- **openEuler / dnf 侧的检查与修复**（`tp.client.sh` → 0.1.4）。
  此前只覆盖了 Debian 的 apt，rpm 系是空的：
  - **metalink**：它返回的是**镜像地址列表**、由 dnf 自己挑 ——
    挑到不在劫持列表里的镜像就绕过缓存了。去掉后走 `baseurl`
    （官方域名，已被劫持）
  - **debuginfo / source / update-source 开着**：普通机器用不到，
    元数据却不小，openEuler 官方默认也是关的
- **保护：只有 metalink 没有 baseurl 的段不删 metalink**（`tp.client.sh` → 0.1.5）。
  删 metalink 的前提是 baseurl 能兜底 —— 某段若只有 metalink，删了等于把
  该仓库彻底去掉。改为**按段判断**（两遍扫描，兼容 baseurl 写在 metalink 之后）

  实测机群：`.19` 791 次 metalink 请求（地址写错，一天到晚 404）、
  `.112/.113/.114` 各 29 次、`.08` 18 次、`.116` 15 次

- 新增 git 全局 `url.*.insteadOf` 重定向的检查与移除 ——
  把 `github.com` 重定向到别处，会让 git 流量绕过 TProxy 缓存
- 发行版判断（`is_debian_like`），apt 相关的检查只对 Debian 系执行

### 修复
- PVE 源按**官方 deb822 格式**重建（`proxmox.sources` / `ceph.sources`，
  `Suites: trixie`，`Signed-By: proxmox-archive-keyring.gpg`）。
  依据 Proxmox 官方 Package Repositories 文档：PVE 9 用 `.sources` 格式，
  旧的 `/etc/apt/sources.list` 应清空

## [0.3.8] - 2026-09-24

### 变更
- Docker 配置改为**标准化**（`tp.client.sh` → 0.1.1）：
  保留 `data-root`，其余键替换成标准值
  （`log-driver` / `log-opts` 50m×5 / `exec-opts` cgroupdriver /
  `storage-driver` overlay2），并移除 `registry-mirrors`
- **`storage-driver` 是条件保留的**：它与 `data-root` 同类 ——
  改了会让 Docker 去别处找镜像层，现有镜像与容器同样会「消失」。
  故本机已是别的驱动时保留原值并告警，不强行改成 overlay2
- 被替换掉的非标准键**逐个列出来**再动手，配置无声消失最难查
- 已是标准时不重写文件（先判断）—— 避免无谓 churn

### 说明
- 实测 7 个常见国内镜像站只有 3 个可用（`docker.1ms.run`、
  `docker.m.daocloud.io`、`docker.imgdb.de`）；
  `docker.1panel.live` 403、`mirror.ccs.tencentyun.com` 无解析（腾讯云内网）、
  `hub-mirror.c.163.com` 无 A 记录（已停用）、`192.168.0.36` 未跑 registry
- ⚠️ `bip` / `mtu` / `default-address-pools` 这类键在某些机器上是有意配的
  （避开网段冲突等），按「其余替换成标准」会被移除 —— 会逐个报告，
  但需要评估是否也该保留

## [0.3.7] - 2026-09-24

### 新增
- 客户端脚本**检查 Docker 的 `registry-mirrors`**（`tp.client.sh` → 0.1.0）。
  配了它的机器，Docker **优先走镜像站**，只有全部失效才回落到
  `registry-1.docker.io` —— 而那个才是被劫持到本地缓存的域名。
  **实测**：在一台配了 5 个镜像站的机器上拉镜像，
  `.18` 的 registry 日志 206 → 206 行，一行都没增加；移除并重启 Docker 后
  同样一次拉取 206 → 212 行
- 范围说明：`registry-mirrors` **只作用于 Docker Hub**；
  ghcr.io / quay.io / gcr.io / nvcr.io / mcr 不受影响，仍走本地缓存

### 修复
- `data-root` **只读不写**。它逐机不同，且改它会让 Docker 换个根目录找数据 ——
  现有容器与镜像会全部「消失」。故检查会把它读出来报告，但任何情况下都不修改。
  同理不整份覆盖 `daemon.json`，只做外科手术式的单项处理
- JSON 非法时**拒绝改写**（宁可不动也不能写坏 —— 写坏了 Docker 起不来），
  且非法 JSON 本身会作为问题报出来。原先解析失败返回空，
  会被误当成「没有 mirrors、无需处理」而静默通过

### 说明
- 实测了 7 个常见国内镜像站，**只有 3 个可用**：
  `docker.1ms.run`、`docker.m.daocloud.io`、`docker.imgdb.de`。
  不可用的：`docker.1panel.live`（403）、`mirror.ccs.tencentyun.com`（无解析，
  腾讯云内网专用）、`hub-mirror.c.163.com`（无 A 记录，已停用）、
  `192.168.0.36`（机器活着但没跑 registry）

## [0.3.6] - 2026-09-23

### 新增
- 劫持并缓存 **`nvcr.io`（NVIDIA NGC）** —— AI 场景的主力仓库
  （CUDA / TensorRT / Triton / NIM / RAPIDS）
- 配套改动：dnsmasq 劫持、域名证书、独立 registry:2 实例（127.0.0.1:5007）、
  缓存目录、客户端 `certs.d` 清单与劫持域名清单（两份实现）。
  **缺任何一样都只有 MITM 成本、没有缓存收益** —— 这条约束写在
  `registry.conf` 的注释里，加仓库时容易被忽略

### 说明
- Docker Hub 与 ghcr 上的 AI 镜像（`ollama/ollama`、`ghcr.io/open-webui`、
  `ghcr.io/huggingface/…`）**本来就在覆盖内**，不需要单独加
- `nvcr.io` 的公开镜像允许匿名拉取（实测），故可共享缓存；
  若某个仓库必须带凭据，则不能共享 —— 与 git 私有仓库同一处理原则

## [0.3.5] - 2026-09-23

### 新增
- 客户端脚本新增**直连模式**（`tp.client.sh` → 0.0.9）：`.18` 不可用时
  用 `--bypass` 把劫持域名钉到源站，机器照常上网；
  `--no-bypass` 撤销。不这么做的话，每次 DNS 查询都要先等 `.18` 超时
  （`resolv.conf` 里配的 2 秒）才落到公网 DNS —— 能通，但每个请求慢一拍。
  源站 IP 于 2026-09-23 用两个公网 DNS 实测（结果一致）并交叉核对；
  `github.com` 给两个（GitHub 自有网络 + Azure 新加坡，后者境内延迟更好）
- 直连块带成对标记，撤销时整体摘掉，不会误伤手工加的记录；
  重新接入（`-i` / `-d`）会自动撤销它并改回走代理

### 修复
- **更正一处未经核实的说法**。此前说 `.19` 的 `/etc/hosts` 是「4 月钉的、
  早过期了」—— 实测：`20.205.243.166` 现在仍返回 HTTP 200，那条记录
  一直可用。真相是 `github.com` 由多个地理分布的负载均衡 IP 提供服务，
  DNS 在**稳定 IP 池内轮换**，不是旧 IP 失效。当初钉新加坡那个
  （Azure，比西雅图近）是有道理的，两年可用也说得通

## [0.3.4] - 2026-09-23

### 新增
- 客户端脚本**检查并修正 `/etc/hosts` 覆盖**（`tp.client.sh` → 0.0.7）。
  nsswitch 里 `files` 排在 `dns` 之前，所以 `/etc/hosts` 的一条记录会稳稳
  压过 DNS 劫持；而自检用的 `dig` 绕过 NSS、看不到它 ——
  表现为「自检全绿、劫持其实完全没生效」，真实程序直连公网。
  **这个坑本项目踩过两次（`.18` 服务端、`.19` 客户端），两次都报的绿灯。**
  实测 `.19` 从接入那天起 github 流量就没走过代理，`git` 缓存一次都没命中

### 修复
- 自检增加「真实解析路径」一环，专门补上 `dig` 看不到 `/etc/hosts` 的盲区
- 修正先判断再动手：`/etc/hosts` 里没命中劫持域名时**一个字节都不动** ——
  像 `raw.githubusercontent.com` 那类不在劫持列表里的记录是人有意配的，不能碰
- 子域判定改用后缀比较。原先用 `index(...) == 长度差`，两串**等长**时
  会得到 `0 == 0` 而误判（`registry.npmjs.org` 与 `repo.openeuler.org`
  同为 18 字符）—— 由测试断言检出条目数时发现

## [0.3.3] - 2026-09-23

### 新增
- 命中率**可点开看未命中明细**：按「路径形态」归并（去掉内容哈希）。
  不归并的话，同一份元数据会因为哈希不同被列成几十条只出现一次的行，
  反而看不出是它在反复回源。点开即可看到 `system` 的头号回源条目

### 修复
- **4xx/5xx 不再计入命中率**，单独列为「错误」。404 不等于「缓存没起作用」——
  那个东西本来就不存在，缓存无从提供，算作未命中就是让指标去回答一个错的问题。
  实测 `.19` 上 openEuler 的 metalink 接口 404 每小时刷 168 次，
  把 `system` 类的命中率从 **70.5% 压到 31.7%**，看起来像缓存坏了，实际毫无问题。
  但**只排除不显示**会走到另一个极端：上游整体 404 这类真故障会被漂亮的
  命中率盖住。所以：不进分母，但单列一栏、非零即标红

## [0.3.2] - 2026-09-23

### 修复
- **客户端脚本的帮助有 57 行** —— 它把整个文件头注释当帮助打印，
  里面是「为什么无参数不执行、DNS 序列为什么是这三个、systemd-resolved
  为什么要关、证书要装四处」这类**维护者的取舍说明**。
  用户要找的那个参数就淹在里面。现帮助只列「能打什么、打了会怎样」：
  `tp.client.sh` 57 → 16 行，`client-setup.sh` 12 行。
  参数一个没动，无参数仍显示帮助
- `client-setup.sh` 的帮助原先用写死的行号 `sed -n '2,12p'` 打印文件头，
  头部注释一增删就会开始打印代码、把 `}` 之类漏给用户。
  改为独立的 `show_help`，与 `dist` 版一致
- 新增 `client/tests/test-help.sh`：守住行数上限、选项齐全、不泄漏代码

## [0.3.1] - 2026-09-23

### 修复
- **管理界面里「要逐字读的东西」比所有标签都小** —— 层级是反的。
  界面上可以小的是标签，不能小的是内容：
  - 更新日志正文 14.5 → 15.5px、行距 1.8；分类标题（新增/修复/变更）14 → 15px
  - 版本条目改用等宽体：版本号与日期是**数据**，与顶栏版本徽标同一种字，
    扫视时一眼能认出分节处
  - 分流配置编辑器 14 → 15px、行距 1.65、可视行数由 13 行增到 20 行。
    nginx 配置符号密集，行距不够极易看串行，而看串行的代价是改错生产配置
  - 展示文件内容的弹层放宽到 820px，配置不再几乎每行都折行
- 标识符（域名、文件名、参数、版本号）原先复用 `--raise` 作底色，
  它与弹层底色相差仅 **1.06:1** —— 看上去像块脏点，不像有意标出的记号。
  新增 `--code-bg` 并把字色由 `--muted` 提到正文色。
  本界面标识符密度极高，它们正是要看清的东西

（对比度本就全部达标 —— 实测最低 6.0:1，均高于 WCAG AA 的 4.5:1。
真正的问题是**字号**，不是颜色。）

### 变更
- 更新日志弹层去掉自带标题：文件第一行就是「# 更新日志」，
  再放一个标题等于同一个词连着出现两遍

## [0.3.0] - 2026-09-23

### 新增
- 界面可**更换根 CA**：生成新的根 CA 并自动重签全部域名证书
- 根 CA 默认有效期由 10 年改为 **30 年**，叶子证书保持 10 年。
  根长叶短：叶子到期重签即可（客户端无感），根 CA 换一次要动每一台客户端
- 换根后自动把新根 CA 发布到客户端分发目录

### 修复
- **客户端脚本换根不彻底**（`tp.client.sh` → 0.0.3）。运行时自带的 CA 包
  （conda / miniconda 的 `ssl/cacert.pem`，它们不读系统信任库）原先按
  **固定标记串**判重，根 CA 一换就误判「已装过」而跳过 ——
  客户端继续握着旧根，表现为「换根后 conda/pip 连不上」，
  且完全看不出是信任锚没换。现标记带上证书指纹，并在追加前先移除旧块
- 客户端脚本同时修正运行时 CA 包的**权限**。conda 的 bundle 可能只剩
  root 可读（早先 `remove` 用 `mktemp` + `mv` 落盘，把临时文件的
  600 权限带了过去），于是普通用户跑 conda/pip 连自己的 CA 包都打不开，
  curl 只报 exit 77，信息里完全看不出是权限问题。
  注意「内容已是最新」那条分支也必须修权限 —— 一台机器只要曾经对过一次，
  之后每次跑都走那条分支，不修就永远好不了
- **`gen-ca.sh` 改为原子替换**（安全）。旧版直接往 `ca/` 里写：
  `openssl genrsa -out tproxy-ca.key` 会立刻截断现有根私钥，
  一旦后续失败就既没有新根也没有旧根 —— 全部客户端与全部域名证书
  同时失效且无从恢复。现改为在工作目录生成后再 `mv` 落位
- `parse_sans` 接受列表入参。换根时 SAN 从原证书读回是列表，
  只认字符串会把 `['a.com']` 连同方括号当域名，导致「所有域名重签失败」
- `_add_server_name` 改为写进**每一段** server 块。原先只改第一段，
  于是域名在 HTTPS 那段不匹配任何 server_name、请求落到 default_server
  被 444 拒掉 —— 表现为「界面提示添加成功，但 https 用不了」
- 同步脚本不再把 `ca/` 下的证书从开发机推向目标机，否则会**静默回退换根**

### 变更
- 新增 `deb.debian.org`、`security.debian.org`、`download.proxmox.com`、
  `enterprise.proxmox.com` 四个上游（PVE / Ceph / Debian 系 apt 源）
- `os-repo.conf` 增加 `/by-hash/` 长缓存：Debian 的 apt 默认开启
  Acquire-By-Hash，元数据走按内容寻址路径，不可变、可长缓存
- 叶子证书默认有效期改回 3650 天（原先一度设为 825 天）

## [0.2.9] - 2026-09-23

### 新增
- **`CHANGELOG.md` 成为版本号的唯一来源**。界面顶栏、`/api/status` 的 `version`
  都从本文件第一条 `## [x.y.z]` 取，改版本号只需改这一处；
  此前版本号写在 `manager/version.py` 里，与文档各记一份，必然会分叉
- 界面**点顶栏版本号可查看本文件**，按 Markdown 渲染（原生 JS 实现，
  不引第三方库；先整体转义再套标记，链接只放行 http/https）

## [0.2.8] - 2026-09-23

### 新增
- 管理界面可**签发与重新签发域名证书**：支持附加域名（SAN）与自定义有效期，
  并自动登记进 `ca/domains.txt`，供根 CA 重建时重签
- 界面单独展示**根 CA** 到期情况。此前它不在证书列表里，而它的失效会让
  所有域名同时失效、且每台客户端都必须重装 —— 最要命的那张反而看不见
- 新增域名默认签 **825 天**而非 3650 天。根长叶短：叶子到期重签即可
  （客户端无感），根 CA 只在最后换一次（客户端必须重装）
- **静态资源指纹自动计算**：`index.html` 里的 `app.js?v=` 不再手写

### 修复
- **`gen-cert.sh` 改为原子替换**（安全）。旧版直接往 `certs/` 里写：
  `openssl genrsa -out` 立刻截断旧私钥，`openssl x509 -req -out` 在报错
  **之前**就把旧证书清成 0 字节。即一次失败的重新签发会让该域名的
  `.crt` 与 `.key` 双双变空、HTTPS 彻底失效，而现场看不出是签发失败造成的。
  现改为在工作目录签好后 `mv` 落位，失败时 `certs/` 一个字节不动。
- 证书有效期不得超过根 CA 剩余期。超过不会报错，只会得到一张
  「界面显示还有很多天、实际从根 CA 过期起就不被信任」的证书
- 前端缓存失效不再依赖人工改版本号 —— 此前忘了改的后果是浏览器一直跑
  缓存的旧脚本，现象却是「新功能没生效」

### 变更
- `ca/domains.txt` 支持一行多域名（首个为 CN，其余为 SAN），
  `proxy/deploy.sh` 的签发循环同步适配

## [0.2.7] - 2026-09-23

### 新增
- 管理端接入 **yz-login 单点登录**，且只放行管理员账号。
  此前管理端没有任何认证，只能靠绑回环地址保护
- 界面可**新增上游**：一次完成 DNS 劫持 + 签发证书 + tengine 分流三处联动
  （漏任何一处都不报错，只是静默不生效）
- 界面可**编辑与新建分流配置**（`tengine/conf.d/*.conf`）
- 客户端分发版 `tp.client.sh`：自包含，供脚本分发平台使用

### 修复
- 带 `ticket` 的回调请求被登录拦截器提前拦掉，导致 SSO 无限重定向
- 修复分发版两处问题：`dig` 包名因发行版而异、CA 轮换不生效

### 变更
- 顶栏显示版本号、登录人、退出入口；整体提高文字对比度、字号加一号
- 劫持规则与证书列表加滚动条

## [0.2.6] - 2026-09-23

### 新增
- 缓存**命中率统计**（解析 nginx 日志的 `cache=HIT/MISS`），
  回答「缓存到底有没有在起作用」—— 这正是本项目最初的痛点

## [0.2.5] - 2026-09-23

### 新增
- 管理界面容器化与总验收（**阶段 8 完成**）

### 修复
- 缓存目录路径映射错误导致 4/6 类被误报为「未启用/空」
  （含 os 缓存已有 75MB 数据却显示未启用）

## [0.2.4] - 2026-09-23

### 新增
- 管理界面前端：缓存总览 / 劫持规则 / 证书 / 分流入口

## [0.2.3] - 2026-09-23

### 新增
- Flask 应用与 API

## [0.2.2] - 2026-09-23

### 新增
- 证书清单与到期检查

## [0.2.1] - 2026-09-23

### 新增
- 缓存占用统计

## [0.2.0] - 2026-09-23

### 新增
- 管理界面骨架与 nginx / dnsmasq 配置只读解析

## [0.1.9] - 2026-09-23

### 新增
- 客户端接入文档（**阶段 7 完成**）

### 修复
- 实机验证发现的 4 个配置缺陷；三个核心库文件曾被 `.gitignore` 规则吞掉

## [0.1.8] - 2026-09-23

### 新增
- 客户端主脚本与实机验证

## [0.1.7] - 2026-09-23

### 新增
- 客户端根 CA 安装（系统信任库 + Docker + Java + 运行时工具各自的自有库）

## [0.1.6] - 2026-09-23

### 新增
- 客户端 DNS 配置（含 `systemd-resolved` 并行查询会绕过劫持的处理）

## [0.1.5] - 2026-09-23

### 新增
- 客户端接入脚本骨架，含发行版与 DNS 机制检测

## [0.1.4] - 2026-09-23

### 新增
- 六类缓存总验收（**阶段 3-6 完成**）

### 修复
- git push 被错误路由进缓存（改为直连）
- 私有仓库经共享缓存无认证开放（改为带凭据流量直连）
- 内容协商串味；补齐无缓存收益的劫持域名

## [0.1.3] - 2026-09-23

### 新增
- Java (Maven) 缓存。元数据与 SNAPSHOT 明确不缓存

## [0.1.2] - 2026-09-23

### 新增
- Node.js (npm) 缓存（处理 gzip 元数据与 `Set-Cookie`）

## [0.1.1] - 2026-09-23

### 新增
- Python (PyPI) 缓存（索引短 TTL / 包文件长 TTL）

## [0.1.0] - 2026-09-23

### 新增
- git 仓库缓存（gitcache）

## [0.0.9] - 2026-09-23

### 新增
- Docker 镜像缓存（registry:2，六个上游实例）

## [0.0.8] - 2026-09-23

### 新增
- 验收与运维文档（**阶段 1-2 完成**）

## [0.0.7] - 2026-09-23

### 修复
- 一键部署与健康检查修复

## [0.0.6] - 2026-09-23

### 新增
- 缓存行为与 302 验证

## [0.0.5] - 2026-09-23

### 新增
- HTTPS 终结（TLS MITM）与按 SNI 取证书

## [0.0.4] - 2026-09-23

### 新增
- tengine 与 HTTP 分流

## [0.0.3] - 2026-09-23

### 新增
- 自建根 CA 与域名证书签发

## [0.0.2] - 2026-09-23

### 新增
- dnsmasq 域名劫持

## [0.0.1] - 2026-09-23

### 新增
- 项目骨架与配置参数
