# 证书私钥 —— 绝不入库
ca/*.key
ca/*.srl
ca/certs/
ca/index.txt*

# 换根时的临时与备份目录 —— 里面是**根 CA 私钥 + 全部域名证书私钥**的副本。
# 回滚失败时备份会留在原地（那是最后的救命稻草），故这条规则必须存在：
# 漏了就等于把整套私钥交给了下一次 git add。
ca/.rotate-*/
ca/.rebuild.*/

# 运行期产物
*.log
logs/

# 根 CA 证书本身。它由**目标机**生成（换根时整个换掉），
# 开发机上的副本永远可能是旧的 —— 留在库里只会形成第二份「看着像真的」
# 的信任锚。分发渠道是 NAS 的发布目录，不是 git。
# 私钥的备份靠离线备份流程，本来就与 git 无关。
ca/tproxy-ca.crt
