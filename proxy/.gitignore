# 证书私钥 —— 绝不入库
ca/*.key
ca/*.srl
ca/certs/
ca/index.txt*

# 换根时的临时与备份目录 —— 里面是**根 CA 私钥 + 全部域名证书私钥**的副本。
# 回滚失败时备份会留在原地（那是最后的救命稻草），故这条规则必须存在：
# 漏了就等于把整套私钥交给了下一次 git add。
ca/.rotate-*/
ca/.rebuild.*/

# 运行期产物
*.log
logs/
