HOST_IP=192.168.0.18
CACHE_BASE=/mnt/HDD/tproxy-cache

# registry:2 多实例共享的 HTTP secret（用于 upload 状态一致性）。
# 仅监听 127.0.0.1，未启用 registry 自身的认证，但仍不应硬编码进 compose。
REGISTRY_HTTP_SECRET=tproxy-shared-secret
